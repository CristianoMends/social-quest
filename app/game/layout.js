export const metadata = {
  title: 'Social Quest — Mapa de Jornada',
}

export default function GameLayout({ children }) {
  return (
    <div className="game-root">
      {children}
    </div>
  )
}
